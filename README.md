# บุ๊คบิ๊ก - แพลตฟอร์มซีรีส์สั้น

แพลตฟอร์มสตรีมมิ่งสำหรับซีรีส์สั้นสมัยใหม่ สร้างด้วย HTML, Tailwind CSS และ vanilla JavaScript ปรับให้เหมาะกับการ deploy บน Vercel

## 🚀 คุณสมบัติ

- **การออกแบบ Responsive**: รองรับมือถือเป็นหลักด้วย Tailwind CSS
- **โหมดมืด**: รองรับธีมสว่าง/มืด
- **UI สมัยใหม่**: เอฟเฟกต์กระจกและแอนิเมชั่นที่ลื่นไหล
- **อินเทอร์เฟซสตรีมมิ่ง**: คล้าย Netflix สำหรับซีรีส์สั้น
- **ประสิทธิภาพสูง**: เบาและโหลดเร็ว

## 🛠️ เทคโนโลยีที่ใช้

- **Frontend**: HTML5, Tailwind CSS (ผ่าน CDN)
- **Icons**: Google Material Icons
- **Fonts**: Google Fonts (Noto Sans Thai, Spline Sans)
- **Deployment**: Vercel (static site)

## 📦 การติดตั้งและพัฒนา

### พัฒนาในเครื่อง

1. Clone  repository
2. ติดตั้ง dependencies:
   ```bash
   npm install
   ```
3. เริ่ม development server:
   ```bash
   npm run dev
   ```
4. เปิด http://localhost:3000

### ดูตัวอย่าง Production

```bash
npm run preview
```

## 🚀 การ Deploy บน Vercel

### การ Deploy อัตโนมัติ (แนะนำ)

1. Push code ขึ้น GitHub
2. Connect repository กับ Vercel
3. Vercel จะ detect เป็น static site อัตโนมัติ
4. Deploy!

### การ Deploy แบบ Manual

1. ติดตั้ง Vercel CLI:
   ```bash
   npm i -g vercel
   ```
2. รันคำสั่ง:
   ```bash
   vercel --prod
   ```

## 📁 โครงสร้างโปรเจค

```
bookbik/
├── index.html          # Main HTML file
├── package.json        # Project configuration
├── README.md          # Documentation
├── .gitignore         # Git ignore file
└── vercel.json        # Vercel configuration (optional)
```

## 🎨 การปรับแต่ง

### เปลี่ยนสีแบรนด์

อัปเดต Tailwind config ใน `index.html`:

```javascript
colors: {
    "primary": "#2b8cee",  // เปลี่ยนสีนี้
    "background-light": "#f6f7f8",
    "background-dark": "#101922",
}
```

### เพิ่มเนื้อหาใหม่

1. เพิ่มรายการภาพยนตร์/ซีรีส์ในส่วนที่เหมาะสม
2. อัปเดต URL ของรูปภาพและข้อมูล
3. ตรวจสอบว่าใช้คลาส responsive แล้ว

## 🔧 การตั้งค่า

### การตั้งค่า Vercel (ไม่จำเป็น)

สร้าง `vercel.json` สำหรับการตั้งค่าแบบกำหนดเอง:

```json
{
  "buildCommand": "echo 'No build needed'",
  "outputDirectory": ".",
  "installCommand": "npm install"
}
```

## 🌐 ตัวแปรสภาพแวดล้อม

นี่เป็น static site ดังนั้นไม่จำเป็นต้องใช้ตัวแปรสภาพแวดล้อม การตั้งค่าทั้งหมดทำผ่านไฟล์ HTML

## 📱 การรองรับเบราว์เซอร์

- Chrome 90+
- Firefox 88+
- Safari 14+
- Edge 90+

## 🤝 การมีส่วนร่วม

1. Fork repository
2. สร้าง feature branch
3. ทำการเปลี่ยนแปลง
4. ทดสอบอย่างละเอียด
5. ส่ง pull request

## 📄 ใบอนุญาต

ใบอนุญาต MIT - ดูไฟล์ LICENSE สำหรับรายละเอียด

## 🆘 การสนับสนุน

สำหรับปัญหาและคำถาม:
- สร้าง issue บน GitHub
- ติดต่อทีมพัฒนา

---

**สร้างด้วย ❤️ สำหรับคนรักซีรีส์สั้น**
